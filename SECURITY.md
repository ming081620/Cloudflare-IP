# 安全策略

## 报告漏洞

请**不要**通过公开 issue 报告安全问题。发邮件到仓库所有者的 GitHub 公开邮箱,
或使用 GitHub 的 Private vulnerability reporting。

请包含:复现步骤(最好是一条 `curl`)、影响范围、以及你认为应该由哪一层拦住它。

## 威胁模型

本项目会**自动往真实 DNS 写记录**,终端用户把流量指向这些域名。因此最严重的漏洞类别是:
让攻击者控制 `<省>.<运营商>.<根域>` 指向的地址。

已实施的分层防御:

1. **Cloudflare 地址段白名单** —— 上传的 IP 必须落在 Cloudflare 公布地址段与客户端 `ip.txt`
   的并集内,写入 DNS 时二次校验。
2. **服务端硬判定** —— 归属地与可信度由 `request.cf` 决定,请求体无法推翻。
   非 CN 或云厂商/IDC 来源一票否决。
3. **节点合理性上限** —— 速度、延迟、丢包、端口、colo 都有界;超界的丢弃或降级。
4. **多设备互证** —— DNS 记录需要来自 ≥2 个独立网络前缀的佐证(或老资格独苗/管理员 pin)。
5. **churn 抑制** —— 替换在位记录需明显更快且在位者已存在一段时间。

`scripts/poc-upload.sh` 是针对第 1–3 层的验收测试。

## 已知的接受项

- **注册是无鉴权的。** 这是产品要求——路由器插件要能开箱即用。滥用由限流、
  按网段的日配额与互证规则约束,而不是靠鉴权。
- **省份代码 `sx`/`sn` 与 GB/T 2260 相反。** 已固化进线上 DNS 记录,不做变更。
- **安装脚本是 `curl | sh` 形式。** 已启用 TLS 校验并提供 SHA256SUMS 校验,
  但仍建议谨慎用户先下载再执行。

## Secrets

`DNS_API_TOKEN` 必须限定为目标 zone 的 `Zone → DNS → Edit`,不要给任何多余权限。
它能改你的 DNS,泄漏它比代码里任何漏洞都危险。

任何 token 都不要写进仓库文件。本地开发用 `.dev.vars`(已 gitignore),
生产用 `npx wrangler secret put`。
